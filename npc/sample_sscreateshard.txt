/**
 * Dynamic item provider.
 * When invoked, the player character can choose from three
 * possible items and it will provide them using SSCreateShard.
 */
prontera.gat,100,100,8	script	QA Tester	50,{
    // Talk to the player; superfluous.
    // Menu for n IDs where IDs are x, y, z.
    // callfunc("SSCreateShard", x|y|z);
    close;
}
